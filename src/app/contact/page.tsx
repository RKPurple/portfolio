'use client'

// Placeholder for the contact section (form + copy). Phase A enables shell + `/contact` routing.
export default function ContactPage() {
    return (
        <div className="flex flex-1 flex-col items-center justify-center text-enter-lettering">
            <p className="font-nav text-lg uppercase tracking-widest opacity-80">Contact</p>
            <p className="mt-2 text-sm opacity-60">Coming soon</p>
        </div>
    )
}
